class Game < ActiveRecord::Base
  attr_accessible :title
  def initialize(params)
    params.each do |key, value|
      puts "the key is " + key.to_s + " the value is " + value.to_s
    end
  end
  def initialize
  end
end
